<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="6dp">

    <!-- Service icon bubble -->
    <FrameLayout
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/bg_cart_icon_circle"
        android:backgroundTint="@color/tint_primary">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_shop"
            app:tint="@color/brand_primary" />
    </FrameLayout>

    <!-- Service name + details -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp">

        <TextView
            android:id="@+id/tvPendingServiceName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-black"
            android:textColor="@color/text_h1"
            android:textSize="14sp"
            android:text="Wash &amp; Fold" />

        <TextView
            android:id="@+id/tvPendingServiceDetails"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:fontFamily="sans-serif-medium"
            android:textColor="@color/text_muted"
            android:textSize="11sp"
            android:text="Shirt ×2  •  Trousers ×1"
            android:maxLines="2"
            android:ellipsize="end" />
    </LinearLayout>

    <!-- Subtotal -->
    <TextView
        android:id="@+id/tvPendingServiceSubtotal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-black"
        android:textColor="@color/brand_primary"
        android:textSize="14sp"
        android:text="₹120" />

</LinearLayout>
